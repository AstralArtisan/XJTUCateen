# Frontend Placeholder

Current MVP uses Thymeleaf pages under `backend/src/main/resources/templates`.

This `frontend/` directory is intentionally reserved for future front-end separation (Vue 3 + Vite) without changing backend domain/service layers.

TODO:
- Create `frontend/` Vue app when moving to separated architecture.
- Reuse existing REST APIs under `/api/**`.
